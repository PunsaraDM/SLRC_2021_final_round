#include <iostream>
#include <bits/stdc++.h>
#include "Generator.h"

#include <algorithm>
#define RED 1
#define GREEN 2
#define BLUE 3
using namespace std;

int main()
{
    // int min_box = 3;
    // int max_box = 6;
    // int box_junction_count = rand() % (max_box - min_box + 1) + min_box;
    // vector<vector<int>> permutations{{2, 2, 2}, {1, 1, 2, 2}, {1, 1, 1, 1, 2}, {1, 1, 1, 1, 1, 1}};
    // int color_order[6] = {RED, RED, GREEN, GREEN, BLUE, BLUE};
    // random_shuffle(std::begin(color_order), std::end(color_order));
    // cout << "box_junction_count"
    //      << "\n";

    // cout << box_junction_count << "\n";

    // int permutation_index = box_junction_count - 3;

    // cout << "permutation_index"
    //      << "\n";
    // cout << permutation_index << "\n";

    // vector<int> order = permutations[permutation_index];
    // for (int i = 0; i < order.size(); i++)
    // {
    //     cout << order[i] << "\n";
    // }
    // cout << "order over" << "\n";
    // for (int i = 0; i < 6; i++)
    // {
    //     cout << color_order[i] << "\n";
    // }
    Generator generator;
    
}